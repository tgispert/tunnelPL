import { PricingSearchNamePipe } from './pricing-search-name.pipe';

describe('PricingSearchNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PricingSearchNamePipe();
    expect(pipe).toBeTruthy();
  });
});
