import { PricingSearchBrandPipe } from './pricing-search-brand.pipe';

describe('PricingSearchBrandPipe', () => {
  it('create an instance', () => {
    const pipe = new PricingSearchBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
